apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "restify.fullname" . }}-test-connection"
  labels:
{{ include "restify.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "restify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
